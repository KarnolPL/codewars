def create_phone_number(n):
    x = str(n[0]) + str(n[1]) + str(n[2])
    y = str(n[3]) + str(n[4]) + str(n[5])
    z = str(n[6]) + str(n[7]) + str(n[8]) + str(n[9])
    result = str('(' + (x) + ')' + ' ' + y + '-' + z)
    return result
